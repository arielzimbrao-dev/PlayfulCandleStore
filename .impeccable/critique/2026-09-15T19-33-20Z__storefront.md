---
target: storefront
total_score: 32
p0_count: 1
p1_count: 2
timestamp: 2026-09-15T19-33-20Z
slug: storefront
---
Method: dual-agent (A: design review · B: detector + browser evidence) — RE-RUN

## Design Health Score

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 2 | PDP rendered 100% blank (P0, now fixed); elsewhere status good |
| 2 | Match System / Real World | 4 | Natural EU-PT; "Default Title" variant leaked (fixed) |
| 3 | User Control and Freedom | 4 | Steppers, Remover, Limpar, breadcrumbs, drawer close |
| 4 | Consistency and Standards | 3 | Placeholder inconsistent (card vs PDP emoji); Default Title leak (fixed) |
| 5 | Error Prevention | 3 | Qty had no upper bound (fixed → max 99); sold-out disabled |
| 6 | Recognition Rather Than Recall | 4 | Persistent filters, breadcrumbs, visible sort |
| 7 | Flexibility and Efficiency | 3 | Sort + multi-select aroma + search; no quick-add from cards |
| 8 | Aesthetic and Minimalist | 3 | Strong system; pink placeholder wall on /produtos monotonous |
| 9 | Error Recognition/Recovery | 2 | Blank PDP was an undiagnosable dead-end (fixed) |
| 10 | Help and Documentation | 4 | FAQ, Envios, Devoluções + PDP accordions |
| **Total** | | **32/40** | **Good (design intent)** |

## Anti-Patterns Verdict
**LLM (A):** NOT AI slop. The "eyebrow above every section" tell is GONE from the home (plain h2 heads); 4 legitimate contextual kickers remain (hero tagline, Playful VIP, chero page-type, PDP product-type). No gradient text (logo is a brand logotype), no side-stripes, no glass cards, no hero-metric template, no overflow. Committed retro-groovy identity (Unbounded, wave dividers, groovy ribbon, autumn leaves, VIP swirl). The one real tell: the branded typographic placeholder becomes a wall of near-identical pink cards on /produtos AND printed the product name twice (fixed) — but that's a content gap (missing photos), not template design.

**Detector (B):** components/app CLEAN (0). global.scss: 2 low-impact `layout-transition` warnings (`transition:width` — carousel dot + free-ship progress bar; the progress bar legitimately needs width). No false positives elsewhere.

**Contrast (B, deterministic):** CTA white-on-deep-coral 5.47 ✓, "Em stock" 4.79 ✓, coral eyebrows 4.88 ✓, body/labels/footer all pass. Two remaining fails (both fixed this pass): branded placeholder text (4.31 at 16px → darkened to coral-deeper) and the amber chero eyebrow over the photo (text-shadow added for over-photo legibility). Tap targets all ≥44px ✓. Mobile: no horizontal scroll on any page ✓; filter toggle works ✓. Console: no real JS errors.

## Priority Issues (all fixed this pass)
- **[P0] PDP rendered blank** — ScrollReveal hid `main>section:not(:first-child)`; on the PDP the first child is the JsonLd `<script>`, so the content section wasn't `:first-child` → stuck at opacity:0 forever (observer never fired). The entire product page (gallery, price, add-to-cart, trust) was invisible = zero conversions. FIXED: changed the exemption to `:not(:first-of-type)` (selects the first `<section>` regardless of preceding scripts) + fail-safe in ScrollReveal. Verified: `opacity:1`, full page renders.
- **[P1] Placeholder printed the product name twice** (in the placeholder + the label below). FIXED: label name hidden when there's no photo.
- **[P1] Listing/PDP imagery weakest surface** — content gap (real photos). Interim branded placeholder in place.
- **[P2] "Default Title" variant label leaked** in cart/drawer. FIXED: suppressed.
- **[P2] Qty stepper had no upper bound.** FIXED: clamped to 99.

## Strengths
1. Committed distinctive retro-groovy identity (zero template feel).
2. Thorough, consistent reassurance system (trust badges + free-ship bar + "Em stock" + toasts across PDP/drawer/cart/empty-cart; empty cart is now a playful peak).
3. Solid a11y post-fixes (contrast passes, focus-visible ring, alt text, keyboard nav, CSS-only mobile filter, ≥44px targets).

## Persona Red Flags (pre-fix)
- Casey/Jordan: first product tap → blank PDP (now fixed).
- Riley: reassurance only reachable on PDP (now reachable); photo-less card wall still reads "placeholder site" until real photos land.

## Questions
1. Should reveal animation ever gate the only conversion page? (Now fixed to fail visible.)
2. Is a whole grid of typographic placeholders honest merchandising, or does it signal "no photos yet"?
3. Where does trust need to start — before the skeptic reaches the good buybox?
